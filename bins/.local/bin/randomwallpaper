#!/usr/bin/bash

wal="$(tree -if ~/pictures/wallpaper | shuf -n 1)"
wal -i "$wal" --saturate 0.75 -a 0.9 -o /home/bibek/.local/bin/dwmcolorsparser.py
# value of alpha from here doesn't reflect in st terminal
